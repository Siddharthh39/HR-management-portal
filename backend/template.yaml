AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: HR Management Portal backend with one Lambda per feature service.

Globals:
  Function:
    Runtime: python3.14
    Timeout: 30
    MemorySize: 512
    Architectures:
      - x86_64
    Environment:
      Variables:
        MYSQL_HOST: management.ctqigesoqgvs.ap-south-1.rds.amazonaws.com
        MYSQL_PORT: "3306"
        MYSQL_USER: admin
        MYSQL_PASSWORD: BamBoo123
        MYSQL_DATABASE: hr_management
        AUTO_CREATE_TABLES: "false"
        ADMIN_BOOTSTRAP_EMAIL: admin@hr.local
        ADMIN_BOOTSTRAP_PASSWORD: ChangeMe123!

Resources:
  HrHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: prod
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - GET
          - POST
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
        AllowHeaders:
          - "*"

  AdminUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: admin_user/
      Handler: app.lambda_handler.handler
      Description: Admin and user management API service.
      Environment:
        Variables:
          API_BASE_PATH: /prod/admin-user
      Events:
        AdminUserRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /admin-user
            Method: GET
        AdminUserRootPost:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /admin-user
            Method: POST
        AdminUserProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /admin-user/{proxy+}
            Method: GET
        AdminUserProxyPost:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /admin-user/{proxy+}
            Method: POST

  LeaveRequestFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: leave_req_submission/
      Handler: app.lambda_handler.handler
      Description: Leave request submission API service.
      Environment:
        Variables:
          API_BASE_PATH: /prod/leave-requests
      Events:
        LeaveRequestRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /leave-requests
            Method: GET
        LeaveRequestProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /leave-requests/{proxy+}
            Method: GET

  OnboardingFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: onbording/
      Handler: app.lambda_handler.handler
      Description: Employee onboarding API service.
      Environment:
        Variables:
          API_BASE_PATH: /prod/onboarding
      Events:
        OnboardingRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /onboarding
            Method: GET
        OnboardingProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /onboarding/{proxy+}
            Method: GET

  PunchInOutFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: punch_in_out/
      Handler: app.lambda_handler.handler
      Description: Punch in and punch out API service.
      Environment:
        Variables:
          API_BASE_PATH: /prod/punch-in-out
      Events:
        PunchInOutRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /punch-in-out
            Method: GET
        PunchInOutProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /punch-in-out/{proxy+}
            Method: GET

  SalaryManagementFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: salary_management/
      Handler: app.lambda_handler.handler
      Description: Salary management API service.
      Environment:
        Variables:
          API_BASE_PATH: /prod/salary-management
      Events:
        SalaryManagementRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /salary-management
            Method: GET
        SalaryManagementProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HrHttpApi
            Path: /salary-management/{proxy+}
            Method: GET

Outputs:
  HrHttpApiEndpoint:
    Description: Base URL for the HR HTTP API stage.
    Value: !Sub "https://${HrHttpApi}.execute-api.${AWS::Region}.amazonaws.com/prod"

  AdminUserBasePath:
    Description: Base path for admin user APIs.
    Value: /admin-user

  LeaveRequestBasePath:
    Description: Base path for leave request APIs.
    Value: /leave-requests

  OnboardingBasePath:
    Description: Base path for onboarding APIs.
    Value: /onboarding

  PunchInOutBasePath:
    Description: Base path for punch in/out APIs.
    Value: /punch-in-out

  SalaryManagementBasePath:
    Description: Base path for salary management APIs.
    Value: /salary-management
